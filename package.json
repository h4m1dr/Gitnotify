{
  "name": "gitnotify",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy && node scripts/setWebhook.js"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "wrangler": "^4.94.0"
  }
}